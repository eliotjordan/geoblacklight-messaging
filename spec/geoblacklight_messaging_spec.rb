require "spec_helper"

describe GeoblacklightMessaging do
  it "has a version number" do
    expect(GeoblacklightMessaging::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
